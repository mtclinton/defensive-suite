{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "defensive-suite console — core app, native notifications, self-update, window-state persistence, and autostart toggle. Response actions (M3) will be added as a separate, narrowly-scoped capability.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "updater:default",
    "window-state:default",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
