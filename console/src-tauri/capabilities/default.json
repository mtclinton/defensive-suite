{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "defensive-suite console — core app + native notifications (read-only). Response actions (M3) will be added as a separate, narrowly-scoped capability.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "updater:default"
  ]
}
